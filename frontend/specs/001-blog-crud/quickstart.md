# 快速開始指南

本指南幫助開發者快速設置部落格 CRUD 系統的開發環境並驗證功能。

## 前置需求

### 必要軟體

- **Node.js**: >= 18.0.0 (推薦使用 18.x LTS)
- **npm**: >= 9.0.0 或 **pnpm**: >= 8.0.0
- **Git**: >= 2.30.0

### 檢查版本

```bash
node --version    # 應顯示 v18.x.x 或更高
npm --version     # 應顯示 9.x.x 或更高
git --version     # 應顯示 2.30.x 或更高
```

### 選擇套件管理器

本專案推薦使用 **pnpm**（更快、更節省空間）：

```bash
# 安裝 pnpm
npm install -g pnpm

# 驗證安裝
pnpm --version
```

---

## 專案設置

### 1. Clone 專案（如果尚未 clone）

```bash
git clone https://github.com/13g7895123/try.blog.git
cd try.blog
```

### 2. 切換到功能分支

```bash
git checkout 001-blog-crud
```

### 3. 安裝相依套件

使用 pnpm（推薦）：
```bash
pnpm install
```

或使用 npm：
```bash
npm install
```

---

## 專案結構

```
try.blog/
├── .specify/               # 規格與開發工具
│   ├── memory/             # 憲章與規範
│   └── scripts/            # 自動化腳本
├── specs/                  # 功能規格文件
│   └── 001-blog-crud/
│       ├── spec.md         # 功能規格
│       ├── plan.md         # 實作計畫
│       ├── research.md     # 技術研究
│       ├── data-model.md   # 資料模型
│       └── contracts/      # API 合約
├── composables/            # Vue Composables
│   ├── useApi.ts           # 資料存取抽象層
│   ├── usePost.ts          # 文章管理
│   └── useTag.ts           # 標籤管理
├── components/             # Vue 元件
│   ├── ArticleEditor.vue   # 文章編輯器
│   ├── ArticleList.vue     # 文章列表
│   ├── ArticleViewer.vue   # 文章檢視器
│   └── TagSidebar.vue      # 標籤側邊欄
├── pages/                  # Nuxt 路由頁面
│   ├── index.vue           # 首頁（文章列表）
│   ├── posts/
│   │   ├── [id].vue        # 文章檢視頁
│   │   ├── new.vue         # 新增文章頁
│   │   └── [id]/edit.vue   # 編輯文章頁
│   └── tags/
│       └── [slug].vue      # 標籤篩選頁
├── layouts/                # Nuxt 版面配置
│   └── default.vue         # 預設版面（含側邊欄）
├── types/                  # TypeScript 類型定義
│   ├── article.ts          # Article 實體
│   └── tag.ts              # Tag 實體
├── utils/                  # 工具函式
│   ├── validation.ts       # 驗證函式
│   └── slugify.ts          # Slug 生成
├── tests/                  # 測試檔案
│   ├── composables/
│   ├── components/
│   └── utils/
├── nuxt.config.ts          # Nuxt 設定
├── tailwind.config.ts      # Tailwind CSS 設定
├── tsconfig.json           # TypeScript 設定
└── package.json            # 專案設定
```

---

## 啟動開發伺服器

### 1. 執行開發模式

```bash
pnpm dev
```

或使用 npm：
```bash
npm run dev
```

### 2. 開啟瀏覽器

開發伺服器啟動後，開啟瀏覽器訪問：

```
http://localhost:3000
```

你應該會看到：
- 空白的文章列表（因為尚無資料）
- 右側的標籤側邊欄（顯示 "尚無標籤"）
- "新增文章" 按鈕

---

## 驗證功能

### 測試 1: 建立第一篇文章

1. **點擊 "新增文章" 按鈕**
   - 應導向 `/posts/new` 頁面

2. **填寫文章內容**：
   ```
   標題: 我的第一篇部落格文章
   
   內容:
   # 歡迎來到我的部落格
   
   這是第一篇測試文章，使用 **Markdown** 格式撰寫。
   
   ## 功能測試
   
   - 支援 Markdown 語法
   - 支援標籤分類
   - 支援日期排序
   
   > 引用測試
   
   ```javascript
   console.log('程式碼區塊測試')
   ```
   
   標籤: 測試, Markdown, 功能
   ```

3. **點擊 "儲存" 按鈕**
   - 應導回首頁
   - 文章列表應顯示剛建立的文章
   - 右側邊欄應顯示 3 個標籤（測試、Markdown、功能）

4. **驗證 localStorage**：
   - 開啟瀏覽器開發者工具（F12）
   - 切換到 "Application" 或 "儲存" 標籤
   - 查看 Local Storage
   - 應看到 `blog:articles` 和 `blog:tags` 兩個鍵值

### 測試 2: 檢視文章

1. **點擊文章標題**
   - 應導向 `/posts/[id]` 頁面
   - 應顯示完整的 Markdown 渲染內容
   - 標題、內容、標籤、建立時間都應正確顯示

2. **驗證 Markdown 渲染**：
   - 標題應渲染為 `<h1>` 和 `<h2>`
   - 粗體文字應正確顯示
   - 項目符號應正確顯示
   - 引用區塊應有灰色背景
   - 程式碼區塊應有語法高亮

### 測試 3: 編輯文章

1. **在文章檢視頁點擊 "編輯" 按鈕**
   - 應導向 `/posts/[id]/edit` 頁面
   - 表單應預填原本的內容

2. **修改內容**：
   - 標題改為: `我的第一篇部落格文章（已更新）`
   - 新增標籤: `更新`

3. **點擊 "儲存" 按鈕**
   - 應導回文章檢視頁
   - 標題應顯示更新後的內容
   - 標籤列表應包含 "更新" 標籤
   - 更新時間應與建立時間不同

### 測試 4: 標籤篩選

1. **在側邊欄點擊 "Markdown" 標籤**
   - 應導向 `/tags/markdown` 頁面
   - 文章列表應只顯示帶有 "Markdown" 標籤的文章

2. **建立更多文章測試篩選**：
   - 建立第二篇文章，標籤設為: `測試, Vue.js`
   - 回到首頁
   - 點擊 "測試" 標籤
   - 應顯示兩篇文章（因為兩篇都有 "測試" 標籤）
   - 點擊 "Vue.js" 標籤
   - 應只顯示第二篇文章

### 測試 5: 刪除文章

1. **在文章檢視頁點擊 "刪除" 按鈕**
   - 應出現確認對話框

2. **確認刪除**
   - 應導回首頁
   - 文章列表應不再顯示該文章
   - 側邊欄標籤計數應更新（若某標籤計數變為 0，應從側邊欄消失）

### 測試 6: 日期排序驗證

1. **建立 3 篇文章**（間隔幾秒）：
   - 文章 A: 標題 "第一篇"
   - 文章 B: 標題 "第二篇"
   - 文章 C: 標題 "第三篇"

2. **驗證排序**：
   - 首頁文章列表應由新到舊排序
   - 順序應為: 第三篇 → 第二篇 → 第一篇

---

## 執行測試

### 單元測試

```bash
pnpm test
```

### 測試覆蓋率

```bash
pnpm test:coverage
```

應達到的覆蓋率目標（參考憲章）：
- **整體覆蓋率**: ≥ 80%
- **關鍵路徑**: ≥ 90%

### 測試特定檔案

```bash
pnpm test composables/usePost.spec.ts
```

---

## 程式碼品質檢查

### Lint 檢查

```bash
pnpm lint
```

### 格式化程式碼

```bash
pnpm format
```

### TypeScript 類型檢查

```bash
pnpm type-check
```

---

## 建置生產版本

### 生成靜態網站

```bash
pnpm generate
```

生成的檔案位於 `.output/public/`，可直接部署到靜態主機。

### 預覽生產版本

```bash
pnpm preview
```

---

## 常見問題排解

### 問題 1: 開發伺服器無法啟動

**症狀**: 執行 `pnpm dev` 後出現錯誤

**解決方案**:
1. 確認 Node.js 版本 >= 18
2. 刪除 `node_modules` 和 `pnpm-lock.yaml`，重新安裝：
   ```bash
   rm -rf node_modules pnpm-lock.yaml
   pnpm install
   ```
3. 檢查 port 3000 是否被佔用，修改 `nuxt.config.ts`：
   ```typescript
   export default defineNuxtConfig({
     devServer: {
       port: 3001 // 改用其他 port
     }
   })
   ```

### 問題 2: localStorage 資料遺失

**症狀**: 重新整理頁面後文章消失

**可能原因**:
- 瀏覽器私密模式（localStorage 被禁用）
- localStorage 被清除

**解決方案**:
1. 確認不是使用私密模式
2. 開啟開發者工具 → Application → Local Storage
3. 檢查 `blog:articles` 和 `blog:tags` 是否存在
4. 若遺失，重新建立文章

### 問題 3: Markdown 渲染錯誤

**症狀**: Markdown 內容顯示為純文字

**解決方案**:
1. 檢查 `marked` 套件是否安裝：
   ```bash
   pnpm list marked
   ```
2. 檢查 `ArticleViewer.vue` 元件是否正確使用 `v-html`
3. 清除瀏覽器快取，重新載入

### 問題 4: 測試失敗

**症狀**: 執行 `pnpm test` 出現錯誤

**解決方案**:
1. 確認 Vitest 套件已安裝
2. 檢查 `vitest.config.ts` 設定
3. 執行單一測試檔案以定位問題：
   ```bash
   pnpm test usePost.spec.ts --reporter=verbose
   ```

### 問題 5: 標籤側邊欄計數錯誤

**症狀**: 標籤顯示的文章數量不正確

**解決方案**:
1. 開啟開發者工具 → Console
2. 執行手動同步：
   ```javascript
   const { fetchTags, getActiveTagsWithCount } = useTag()
   await fetchTags()
   const tags = await getActiveTagsWithCount()
   console.log(tags)
   ```
3. 若資料不一致，可能需要重建 localStorage 資料：
   ```javascript
   localStorage.clear()
   ```
   然後重新建立文章

---

## 效能驗證

### 檢查載入時間

1. 開啟開發者工具 → Network 標籤
2. 重新整理頁面
3. 驗證指標：
   - **首次內容繪製 (FCP)**: < 1.5s
   - **最大內容繪製 (LCP)**: < 2.5s
   - **累積版面配置偏移 (CLS)**: < 0.1

### 檢查 Bundle 大小

```bash
pnpm build --analyze
```

驗證目標（參考 research.md）：
- **JavaScript bundle**: < 200KB (gzip)
- **CSS bundle**: < 50KB (gzip)

---

## localStorage 資料管理

### 匯出資料

```bash
# 開啟瀏覽器 Console，執行：
const data = {
  articles: JSON.parse(localStorage.getItem('blog:articles')),
  tags: JSON.parse(localStorage.getItem('blog:tags'))
}

const json = JSON.stringify(data, null, 2)
const blob = new Blob([json], { type: 'application/json' })
const url = URL.createObjectURL(blob)

const a = document.createElement('a')
a.href = url
a.download = 'blog-backup.json'
a.click()
```

### 匯入資料

```bash
# 開啟瀏覽器 Console，執行：
const fileInput = document.createElement('input')
fileInput.type = 'file'
fileInput.accept = 'application/json'

fileInput.onchange = async (e) => {
  const file = e.target.files[0]
  const text = await file.text()
  const data = JSON.parse(text)
  
  localStorage.setItem('blog:articles', JSON.stringify(data.articles))
  localStorage.setItem('blog:tags', JSON.stringify(data.tags))
  
  location.reload()
}

fileInput.click()
```

### 清除所有資料

```bash
# 開啟瀏覽器 Console，執行：
const keys = Object.keys(localStorage)
keys.filter(k => k.startsWith('blog:')).forEach(k => {
  localStorage.removeItem(k)
})

location.reload()
```

---

## 下一步

完成快速開始驗證後，你可以：

1. **閱讀完整規格**：
   - `specs/001-blog-crud/spec.md` - 功能規格
   - `specs/001-blog-crud/data-model.md` - 資料模型
   - `specs/001-blog-crud/contracts/` - API 合約

2. **開始開發**：
   - 參考 `plan.md` 的實作計畫
   - 按照 Task 順序開發功能

3. **執行測試**：
   - 參考憲章的測試標準
   - 確保測試覆蓋率達標

4. **提交變更**：
   - 遵循 Git commit 規範
   - 提交 PR 前確保 lint 和測試通過

---

## 取得協助

如果遇到問題：

1. **查看規格文件**：`specs/001-blog-crud/`
2. **查看憲章**：`.specify/memory/constitution.md`
3. **執行診斷腳本**（如有提供）
4. **檢查 Git issues**
5. **聯繫團隊成員**

---

## 開發工具建議

### VS Code 擴充套件

推薦安裝以下擴充套件：

- **Vue Language Features (Volar)**: Vue 3 語法支援
- **TypeScript Vue Plugin (Volar)**: TypeScript 整合
- **Tailwind CSS IntelliSense**: Tailwind 自動完成
- **ESLint**: 程式碼品質檢查
- **Prettier**: 程式碼格式化

### 設定 VS Code

在專案根目錄建立 `.vscode/settings.json`：

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "typescript.tsdk": "node_modules/typescript/lib"
}
```

---

**恭喜！** 你已經完成開發環境設置並驗證了核心功能。開始享受開發吧！🎉
